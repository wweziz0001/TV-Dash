# Cross-Workspace Tests

This folder is reserved for future end-to-end, contract, or multi-workspace regression suites that exercise both `apps/api` and `apps/web` together.

Do not place app-local unit tests here.
